# Meme annotation pipeline (ScrapeGraph-AI based).
# Install:  pip install -r requirements-annotation.txt
# ScrapeGraph-AI renders pages with Playwright; after install run:
#     playwright install chromium

scrapegraphai>=1.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Optional MongoDB driver for loading annotations programmatically.
pymongo>=4.6.0
