# Meme GNN+ILP student model (training, inference, dataset building).
# Install:  pip install -r requirements-model.txt
#           playwright install chromium      # for live rendering (build_meme_dataset / infer --url)

torch>=2.0.0
torch-geometric>=2.3.0
ortools>=9.6
scikit-learn>=1.2.0
numpy>=1.24.0
playwright>=1.40.0
requests>=2.28.0   # proxy pool fetch (src/net/proxy_pool.py)
